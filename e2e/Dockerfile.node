FROM debian:trixie-slim
RUN apt-get update && apt-get install -y ca-certificates bash coreutils tmux && rm -rf /var/lib/apt/lists/*

COPY target/release/webmux-node /usr/local/bin/

CMD ["webmux-node"]
