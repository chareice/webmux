{
  "identifier": "mobile",
  "description": "Capabilities for Android/iOS. The WebView loads the hub directly, so plugin access is scoped to that origin: build.rs fills in `remote.urls` from OFFDESK_MOBILE_HUB_URL (plus the Android emulator's host alias) before tauri-build reads this file. Editing `remote.urls` here has no effect.",
  "platforms": ["android", "iOS"],
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default",
    "shell:allow-open",
    "opener:default",
    "clipboard-manager:allow-read-text",
    "clipboard-manager:allow-write-text",
    "process:default"
  ]
}
